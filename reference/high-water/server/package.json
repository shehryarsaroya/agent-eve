{
  "name": "highwater-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "High Water — the AI-agent flood-basin game. Backend engine + agent API + spectator stream.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
